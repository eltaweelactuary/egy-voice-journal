# المفاتيح -- لا تُرفع أبدًا
.env

# ملفات مؤقتة من عملية التفريغ
journal/.work/
*.pyc
__pycache__/
.venv/
venv/

# لو تحب تستخدم مجلد inbox كمنطقة تجهيز محلية بدون رفع الصوت للمستودع،
# أزل التعليق عن السطرين التاليين. لكن انتبه: لو فعّلتهما فلن يعمل
# تشغيل الـ workflow تلقائيًا عند دفع ملف إلى inbox، ويبقى طريق الرابط
# (audio_url) هو الطريق الوحيد -- وهو الطريق المنصوح به أصلًا.
# inbox/*
# !inbox/.gitkeep
