# مخطط تنفيذي — خط أنابيب أوتوماتيكي من الألف للياء على AWS

**لمن يقرأ:** أنت الموديل المنفِّذ. هذه مواصفة، لا اقتراح. اقرأ القسم ٠ قبل
أي شيء — فيه القيد الذي ستخطئ فيه افتراضيًا وسيكلّف المالك أيامًا.

اقرأ أيضًا `HANDOFF.md` (السياق والقرارات السابقة) و`s3_pipeline.py` (الكود
الموجود الذي تبني عليه، لا تعيد كتابته).

---

## ٠. القيد الحاكم — اقرأه أولًا

**لا تصمّم تشغيلًا متوازيًا لكل ملف.**

الغريزة الافتراضية هي: حدث S3 لكل ملف ← Lambda لكل ملف ← معالجة متوازية.
هذا **سيفشل فورًا** هنا، لسبب مقيس:

- المالك سيرفع **دفعة كبيرة مرة واحدة** (قال ذلك صراحةً). عشرون ملفًا معًا
  تعني عشرين طلبًا متوازيًا.
- الطبقة المجانية لـ Gemini حدّها **١٠ طلبات/دقيقة و٢٥٠/يوم**. التوازي
  يفجّرها فورًا فتحصل على عاصفة 429.
- وهذا **حدث فعلًا** في تشغيل حقيقي: سلسلة الموديلات استهلكت ١٠١٢ ثانية
  على ملف واحد بسبب 503 متكرر. تخيّلها مضروبة في عشرين بالتوازي.

**التصميم الصحيح: عامل واحد يسحب من طابور ويعالج بالتتابع، مع مُحدِّد معدل.**
التوازي هنا ليس تحسينًا، بل عطب.

قيد ثانٍ: **at-least-once**. SQS القياسي قد يسلّم الرسالة مرتين، وإعادة
المحاولة أمر طبيعي. لذلك **الإدمبوتنسي إلزامية** — وهي منفَّذة أصلًا في
`s3_pipeline.py` (يتخطّى الملف إن وُجد `journal/<متصل>/<stem>.json`). لا
تُزلها.

---

## ١. المعمارية

```
 ┌─ جهاز:   aws s3 sync ./recordings s3://BUCKET/inbox/<متصل>/
 └─ موبايل: صفحة رفع presigned  (بلا أي مفتاح AWS على الموبايل)
                    │
                    ▼
        S3  (خاصة، SSE، حجب وصول عام كامل)
            inbox/<متصل>/<ملف>
                    │  s3:ObjectCreated:*  على البادئة inbox/
                    ▼
              EventBridge  ──▶  SQS journal-ingest
                    │                    │ بعد ٣ محاولات
                    │                    ▼
                    │              SQS journal-ingest-dlq  ──▶ تنبيه
                    ▼
        Lambda مُشغِّل (صغيرة، لا تعالج شيئًا)
          └─ هل يوجد عامل يعمل الآن؟ نعم ← لا تفعل شيئًا
                                     لا  ← ECS RunTask (عامل واحد)
                    │
                    ▼
        العامل: Fargate task واحد — يستنزف الطابور بالتتابع ثم يخرج
          ├─ ffmpeg: 16kHz mono opus  (~١١–١٢ ميجا/ساعة)
          ├─ تقطيع عند الصمت (لا عند زمن ثابت)
          ├─ ASR: سلسلة مزوّدين + مُحدِّد معدل + سياسة فشل
          ├─ تطبيع عربي (مع الاحتفاظ بالنص الخام كما هو)
          ├─ journal/<متصل>/{md,txt,srt,json}
          ├─ summaries/<متصل>.md      (إعادة بناء)
          ├─ index.json               (الفهرس الذي تقرأه العمليات)
          └─ processed/<متصل>/<ملف>   (يُحذف بعد ٣٠ يومًا)
                    │
                    ▼
        جاهز للعمليات: index.json + journal/*.json + summaries/*.md
```

**لماذا Lambda مُشغِّل + جدول احتياطي، لا خدمة دائمة؟**
خدمة ECS بـ `desiredCount=1` تعمل ٢٤/٧ وتكلّف ~٩ دولارات شهريًا بلا عمل.
المُشغِّل يجعل الاستجابة شبه فورية عند الرفع، والتكلفة صفرًا وقت السكون.
أضف **جدول EventBridge كل ١٥ دقيقة** كشبكة أمان: يلتقط أحداثًا ضائعة ويعيد
تشغيل ما تعطّل، ويخرج فورًا إن كان الطابور فارغًا.

---

## ٢. الموارد المطلوبة بالاسم

استخدم منطقة واحدة لكل شيء. اقترح `us-east-1` إلا إن كانت EC2 الحالية في
غيرها — حينها طابِق منطقتها لتوفير نقل البيانات.

| المورد | الاسم | إعدادات إلزامية |
|---|---|---|
| S3 | `egy-voice-journal-audio-075298365868` | حجب وصول عام كامل، SSE-S3، lifecycle: حذف `processed/` بعد ٣٠ يومًا، إلغاء الرفع غير المكتمل بعد ٧ أيام |
| SQS | `journal-ingest` | `VisibilityTimeout=3600` (أطول من أقصى زمن معالجة)، retention ١٤ يومًا |
| SQS | `journal-ingest-dlq` | `maxReceiveCount=3` على الطابور الأصلي |
| ECR | `egy-voice-journal-worker` | صورة العامل |
| ECS | عنقود `journal` + Task Definition `journal-worker` | Fargate، ١ vCPU / ٤ جيجا (٨ جيجا لو ستشغّل whisper محليًا) |
| Lambda | `journal-trigger` | Python 3.12، timeout ٣٠ث، بلا تبعيات خارجية |
| EventBridge | قاعدة `journal-safety-net` | `rate(15 minutes)` ← نفس الـ RunTask |
| Secrets Manager | `journal/api-keys` | `GEMINI_API_KEY`, `GROQ_API_KEY`, `ELEVENLABS_API_KEY` |
| CloudWatch | تنبيه على `ApproximateNumberOfMessagesVisible` في الـ DLQ > 0 | إشعار للمالك |

**الأسرار في Secrets Manager أو SSM SecureString — لا في متغيّرات بيئة
Task Definition**، فهي تظهر نصًا صريحًا في الكونسول ولأي من له صلاحية قراءة
التعريف.

### صلاحيات IAM — أقل صلاحية، ثلاثة أدوار

1. `journal-worker-task-role`
   - `s3:GetObject,PutObject,DeleteObject,ListBucket` على **هذه الحاوية فقط**
   - `sqs:ReceiveMessage,DeleteMessage,ChangeMessageVisibility,GetQueueAttributes`
     على `journal-ingest` فقط
   - `secretsmanager:GetSecretValue` على `journal/api-keys` فقط
   - `logs:CreateLogStream,PutLogEvents`
2. `journal-trigger-lambda-role`
   - `ecs:RunTask` على `journal-worker` فقط، `ecs:ListTasks`
   - `iam:PassRole` على دور التنفيذ ودور المهمة فقط
   - `logs:*` على مجموعتها
3. الموبايل: **لا دور ولا مفتاح.** روابط presigned فقط.

**لا تستخدم بروفايل `kirocrew-automation`** — مقصور على EC2 ولا يستطيع إنشاء
S3. خطوات الإنشاء تحتاج `default`.

---

## ٣. المراحل — كل مرحلة قابلة للاختبار بذاتها

نفّذ بالترتيب. لا تنتقل قبل تحقّق معيار القبول.

### المرحلة ٠ — الأساس
ادمج PR #4 أولًا: يحتوي `s3_pipeline.py` (منطق السحب والمعالجة والتجميع
والإدمبوتنسي) وإصلاح `max_retries`. **كل ما يلي يبني عليه.**

**قبول:** `python s3_pipeline.py setup --profile default` ينشئ الحاوية،
و`aws s3 ls` يعرض البادئات الأربع.

### المرحلة ١ — الرفع
- **دفعة من الجهاز:** أمر واحد موثّق:
  `aws s3 sync "<مجلد>" "s3://BUCKET/inbox/<متصل>/" --exclude "*" --include "*.m4a" --include "*.mp3"`
- **من الموبايل:** Lambda Function URL تُصدر presigned POST محدود بـ
  ١٥ دقيقة و١٥٠ ميجا، وصفحة HTML ساكنة تستهلكها. احمِ الـ URL بسرّ طويل في
  المسار كحد أدنى؛ Cognito أفضل لو أراد المالك.
  - **بديل فوري بلا بناء:** تطبيق AWS Console للموبايل يرفع إلى S3 اليوم.
    استخدمه للتحقق من بقية الخط قبل بناء الصفحة.
  - **أفضل تجربة:** iOS Shortcut / Android يشارك من مسجّل الصوت مباشرة
    إلى نقطة الـ presigned.
- طبّع أسماء الملفات: أسماء المسجّلات الحقيقية فيها إيموجي وأرقام
  عربية-هندية ومحارف اتجاه، وهي تكسر مفاتيح S3. الدالة موجودة:
  `s3_pipeline.safe_name()`.

**قبول:** ملف من الموبايل وملف من الجهاز يظهران تحت
`inbox/<متصل>/` بأسماء سليمة، والحاوية ما زالت غير عامة.

### المرحلة ٢ — الطابور
S3 → EventBridge → SQS، مع DLQ. استخدم EventBridge لا إشعار S3 المباشر:
قواعد بادئات S3 لا تتداخل بمرونة، وEventBridge يسمح بتصفية الامتدادات
وبإضافة مستهلكين لاحقًا.

**قبول:** رفع ملف يُنتج رسالة واحدة في `journal-ingest` خلال ثوانٍ. رفع
ملف غير صوتي لا يُنتج شيئًا. رسالة تفشل ٣ مرات تنتهي في الـ DLQ.

### المرحلة ٣ — العامل (جوهر العمل)
صورة Docker: `python:3.12-slim` + `ffmpeg` + `boto3 requests` + المستودع.

منطق العامل — حلقة استنزاف:
```
احضر الأسرار مرة واحدة
طالما (رسائل متبقية و لم ينتهِ حد الزمن):
    استقبل رسالة (long-poll 20s)
    لا رسائل ← اخرج بنجاح
    استخرج المفتاح، استنتج المتصل
    موجود journal/<متصل>/<stem>.json ؟ ← احذف الرسالة واستمر (إدمبوتنسي)
    مدِّد المهلة دوريًا أثناء المعالجة (heartbeat)
    عالِج عبر transcribe_file()
    نجح  ← ارفع المخرجات، انقل الصوت إلى processed/، أعد بناء الملخص،
            حدّث index.json، احذف الرسالة
    فشل  ← لا تحذف الرسالة. طبّق سياسة الفشل أدناه.
```

**مُحدِّد المعدل — إلزامي:**
- حد أدنى ٦ ثوانٍ بين طلبات Gemini (١٠/دقيقة).
- عدّاد يومي في `state/quota-YYYY-MM-DD.json` داخل الحاوية لاحترام ٢٥٠/يوم.
  عند النفاد: أوقف العمل بهدوء واترك الرسائل للغد. لا تحرقها.
- بما أن التزامن واحد، مُحدِّد داخل العملية يكفي. لا تحتج DynamoDB.

**سياسة الفشل — اجعلها متغيّر بيئة `FALLBACK_POLICY`:**
- `requeue` (الافتراضي): ازدحام 503/429 لا يعني تفريغًا أسوأ. أعد الرسالة
  للطابور بمهلة تصاعدية وحاول لاحقًا بنفس المزوّد الجيد.
- `degrade`: اسقط لمزوّد أقل دقة فورًا.

هذا قرار جوهري: القياس الحقيقي أظهر أن `groq` يعمل في ٣ ثوانٍ لكن **جودته
ضعيفة** — بدأ مخرَجه بـ «مرحباً. مرحباً. يا عبد البر» وهو كلام لم يُقَل،
وبالفصحى لا بالعامية. فالسقوط التلقائي إليه يشتري سرعة بثمن الدقة، وهي
الشيء الذي يريده المالك. الافتراضي `requeue`.

**قبول:** رفع ٥ ملفات معًا. تُعالج بالتتابع لا بالتوازي (اثبت ذلك من
سجلات CloudWatch)، ولا ظهور لـ 429. إعادة رفع نفس الملف لا تُنتج تفريغًا
ثانيًا. قتل المهمة في منتصف العمل يؤدي لإعادة المحاولة لا لفقدان الملف.

### المرحلة ٤ — مخرجات جاهزة للعمليات
هذا ما يعني «تفريغ كامل جاهز للعمليات» — عقد صريح:

1. `journal/<متصل>/<stem>.json` — كما هو الآن، **مضافًا إليه** لكل سطر
   حقل `normalized` بجانب `text`.
2. `summaries/<متصل>.md` — مجمّع مرتّب زمنيًا (موجود).
3. **`index.json` في جذر الحاوية** — الملف الوحيد الذي تقرأه أي عملية
   لاحقة، فلا تحتاج سرد S3:
```json
{
  "generated": "2026-09-17T11:00:00Z",
  "totals": {"recordings": 42, "hours": 18.3, "words": 154200},
  "callers": [
    {"caller": "جمال Konecta", "recordings": 7, "hours": 3.1,
     "summary_key": "summaries/جمال Konecta.md",
     "items": [{"stem": "...", "date": "...", "duration": 1740,
                "words": 3120, "provider": "gemini",
                "json_key": "journal/.../x.json"}]}
  ]
}
```

**التطبيع العربي — احتفظ بالاثنين.** وحّد الألف (ا أ إ آ) والهمزة والتاء
المربوطة/الهاء، واحذف التشكيل والتطويل — في حقل `normalized` **فقط**.
النص الخام يبقى حرفيًا كما نُطق، لأن عاميّة المالك هي المُنتَج نفسه ولا
تُمسّ. التطبيع للبحث والمطابقة، لا للعرض.

**قبول:** `index.json` يطابق ما في الحاوية فعلًا. البحث عن «إزاي» يجد
«ازاي» و«إزّاي». النص المعروض ما زال بالعامية بلا تغيير.

### المرحلة ٥ — المراقبة والتكلفة
- تنبيه CloudWatch: أي رسالة في الـ DLQ ← إشعار.
- `state/last-run.json`: وقت آخر تشغيل، عدد المعالَج، عدد الفاشل.
- ميزانية AWS Budget بحد ٥ دولارات شهريًا مع تنبيه — شبكة أمان ضد أي خطأ
  يشغّل موارد بلا توقف.

**قبول:** إجبار فشل يوصل تنبيهًا للمالك. الميزانية مُفعَّلة.

---

## ٤. التكلفة المتوقعة

أرقام تقديرية لاستخدام شخصي (~٢٠ ساعة صوت شهريًا). تحقّق منها بحاسبة AWS.

| البند | تقدير شهري |
|---|---|
| S3 تخزين (تفريغ + صوت ٣٠ يومًا) | < ٠٫٥٠ دولار |
| SQS | فعليًا صفر (المليون طلب الأولى مجانًا) |
| Fargate (تشغيل عند الحاجة فقط) | ٠٫٣٠ – ١٫٥٠ دولار |
| Lambda + EventBridge | فعليًا صفر |
| Secrets Manager | ٠٫٤٠ دولار لكل سرّ |
| التفريغ عبر Gemini المجاني | صفر |
| **المجموع** | **~١ – ٣ دولارات** |

لو استُخدم Gemini المدفوع (لا تدريب على بياناتك): أضف ~١٠ سنتات لكل ساعة صوت.

---

## ٥. ما لا تفعله

- **لا توازي.** انظر القسم ٠.
- **لا تحذف الصوت الخام فورًا** — انقله إلى `processed/` ودع lifecycle يحذفه.
  إعادة التفريغ بمزوّد أفضل تحتاج الأصل.
- **لا تضع مفاتيح AWS على الموبايل.** presigned فقط.
- **لا تجعل الحاوية عامة** ولا تضف سياسة قراءة عامة «مؤقتًا».
- **لا تُزل الضغط ولا التقطيع عند الصمت ولا الـ prompt المانع للتفصيح** —
  كلها قرارات مقيسة، أسبابها في `HANDOFF.md` القسم ٢.
- **لا تدفع إلى `main` مباشرة** — الفرع محميّ. فرع + PR.

---

## ٦. قرارات مفتوحة تحتاج المالك

1. **المزوّد النهائي غير محسوم.** `qwencleo` (المتخصص في المصري) **لم
   يُختبر بعد** وهو محور المشروع. اختباره يحتاج GPU (Colab مجاني موجود،
   أو EC2 spot). لو فاز، فالعامل يحتاج GPU وتتغير حسبة التكلفة جوهريًا:
   `g4dn.xlarge` spot بدل Fargate. **شغّل `compare.py` أولًا وقرّر بعده.**
2. المنطقة: هل EC2 الحالية في `us-east-1`؟ طابقها.
3. زمن الاستجابة المقبول: المُشغِّل يجعله ثوانٍ. لو الجدول وحده كافٍ
   (١٥ دقيقة) فاحذف Lambda والمُشغِّل واكتفِ بالجدول — أبسط وأرخص.
4. هل يريد الملخصات أن تُدفع إلى المستودع أيضًا، أم تبقى في S3 فقط؟
   محتواها شخصي؛ الافتراضي: S3 فقط.

---

## ٧. اختبار القبول النهائي

سلسلة واحدة تثبت «من الألف للياء»:

1. من الموبايل: ارفع تسجيلًا لمتصل جديد.
2. من الجهاز: `aws s3 sync` لمجلد فيه ٤ تسجيلات لمتصلين مختلفين.
3. لا تلمس شيئًا بعد ذلك.
4. تحقّق خلال ٢٠ دقيقة:
   - `inbox/` فارغ، `processed/` فيه الخمسة.
   - `journal/` فيه ٤ ملفات لكل تسجيل.
   - `summaries/` فيه ملف لكل متصل، مرتّب زمنيًا.
   - `index.json` صحيح ومطابق.
   - السجلات تُظهر معالجة **متتابعة** وصفر 429.
   - الحاوية غير عامة، والصوت مشفّر.
5. أعد رفع نفس الملفات: لا تفريغ مكرّر، ولا تكلفة إضافية.
