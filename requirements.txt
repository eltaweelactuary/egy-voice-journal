# المتطلبات الأساسية -- تكفي لكل مزودي الـ API
requests>=2.31.0

# للوضع المحلي فقط (whisper بدون أي مفتاح وبدون تكلفة):
#   pip install faster-whisper
# لا نثبّته افتراضيًا لأنه يجرّ حوالي 1.5 جيجا من أوزان الموديل.

# للمحرك المتخصص في المصري (qwencleo) -- يحتاج GPU ليكون عمليًا:
#   pip install torch torchaudio
#   pip install qwencleo-asr --no-deps
#   pip install "qwen-asr>=0.0.6" numpy soundfile huggingface_hub

# لأتمتة S3 فقط (s3_pipeline.py):
#   pip install boto3
