# برومت التسليم — اقرأه كاملًا قبل أن تكتب سطرًا واحدًا

أنت تستلم مشروعًا نصف مبنيّ. هذا الملف هو كل ما تحتاجه: السياق، والقرارات
التي اتُّخذت ولا يُعاد فتحها، وما تم التحقق منه فعليًا، وما بقي. اقرأه كله
أولًا — أغلب أخطاء التسليم تحدث لأن المستلم أعاد بناء ما كان جاهزًا، أو نقض
قرارًا اتُّخذ لسبب مقيس.

---

## ١. الهدف

مالك المشروع يسجّل تسجيلات صوتية شخصية ومكالمات **بالعامية المصرية**،
ويريد:

1. **تفريغها نصًا بأقصى دقة ممكنة على العامية المصرية تحديدًا** — لا الفصحى.
2. **الوصول للمنظومة من أي مكان** (متصفح أو موبايل) بلا تثبيت أي شيء.
3. **مجانًا، أو على AWS بتكلفة زهيدة.**
4. **مناقشة النصوص الناتجة** بعد ذلك ليفهم نفسه ويستخرج أنماطًا متكررة.

أحجام الملفات: تسجيلات موبايل عادية، حتى ١٠٠ ميجا، وغالبًا ~٦٠ ميجا.

---

## ٢. قرارات نهائية — لا تنقضها بلا سبب مقيس

هذه ليست تفضيلات، كل واحدة لها سبب. لو أردت تغيير أي منها، اقس أولًا وأثبت.

### أ) Whisper ليس المحرك المناسب للعامية المصرية

على نفس مجموعة الاختبار المصرية:

| الموديل | WER |
|---|---|
| QwenCleo-ASR (1.7B) | **19.85%** |
| Arabic-Whisper-CodeSwitching | 37.98% |
| Nemotron / Cohere | ~38% |

المحرك الافتراضي الآن `qwencleo`. **لا تُرجعه إلى Whisper.**

> تحذير أمانة: رقم QwenCleo **يقيسه صاحب الموديل على مجموعته الخاصة**
> وبتطبيعه الخاص، والمشروع حديث (مؤلف واحد). أما ElevenLabs Scribe v2 فرقمه
> **١٣٫١٪ على المصري من ورقة مستقلة على arXiv** — دليل أوثق لكنه مدفوع.
> الرقمان غير قابلين للمقارنة (مجموعتا اختبار مختلفتان). لهذا وُجد
> `compare.py`: القرار يُحسم على صوت المالك لا على بنشمارك.

### ب) الضغط الذكي هو ما يحل مشكلة الحجم — لا الدفع

كل محركات ASR تُخفّض الصوت داخليًا إلى 16kHz أحادي القناة. فتحويله مسبقًا
إلى Opus 24kbps mono 16kHz **لا يُفقد دقة تفريغ إطلاقًا** ويقلّص الحجم بشدة.

قياس فعلي أُجري:

```
23.9 MB  ->  6.4 MB      على ملف اختبار مُصنَّع (128kbps ستيريو)
المخرَج الموثوق: ~11-12 MB لكل ساعة صوت
```

> **اقرأ هذا التحفّظ:** نسبة التوفير تتبع بتريت المصدر، لا إعداداتنا — تسجيل
> موبايل 64kbps أحادي سيوفّر أقل بكثير من ٧٣٪. الرقم الذي يُعتمد عليه هو
> **المخرَج المطلق ~١١–١٢ ميجا لكل ساعة**، لأنه ثابت بحكم إعداداتنا. ولم
> يُختبر ملف حقيقي بحجم ١٠٠ ميجا قط. انظر القسم ٩.

أي أن ساعتين من الصوت تصيران ~٢٥ ميجا، فتدخل تحت حد Groq المجاني.
**لا تُزل خطوة الضغط.**

### ج) القطع عند الصمت، لا عند زمن ثابت

القطع في منتصف كلمة هو السبب الأول لأخطاء التفريغ. `detect_silences()` تكشف
فترات الصمت و`plan_chunks()` تقطع عند أقربها للهدف (٩٠٠ ثانية، بحد أقصى
١٢٠٠). تم التحقق: القطعان وقعا عند صمت فعلي.

### د) الـ prompt يمنع «تفصيح» الكلام

أشهر فشل في تفريغ العامية أن الموديل يحوّل «مش عارف أعمل إيه» إلى «لا أعرف
ما أفعل» فتضيع نبرة المتكلم. `build_prompt()` يمنع ذلك صراحةً ويطلب النقل
الحرفي. **لا تُلطّف هذه التعليمات ولا تختصرها.**

### هـ) تمرير السياق بين المقاطع

كل مقطع يُرسَل مع آخر ~٤٠٠ حرف من المقطع السابق، فلا ينقطع تسلسل الكلام عند
الحدود. الحقل اسمه `tail`.

---

## ٣. ما هو مبنيّ ومُتحقَّق منه

كل ملفات بايثون تُترجم بلا أخطاء، وكل YAML صالح. تم التحقق فعليًا.

| الملف | الوظيفة | الحالة |
|---|---|---|
| `transcribe.py` | القلب: ضغط + كشف صمت + تقطيع + ٥ محركات + إخراج | مُختبر فعليًا على صوت |
| `ask.py` | مناقشة الأرشيف عبر Gemini بنافذة سياق كبيرة | يُترجم، **لم يُختبر بمفتاح حقيقي** |
| `compare.py` | مقارنة المحركات على عيّنة من صوت المالك | **شُغِّل فعلًا** — انظر أدناه |
| `s3_pipeline.py` | أتمتة S3: سحب ← تفريغ ← تجميع ملخص لكل متصل ← رفع | منطق الأسماء مُختبر، **لم يُشغَّل على AWS** |
| `colab/transcribe_colab.ipynb` | تشغيل بلا جهاز محلي على GPU مجاني | شُغِّل، وأُصلح فيه باگ مسار متداخل |

### ما أثبته أول تشغيل حقيقي (لا تُعِد اكتشافه)

شُغِّل `compare.py` على مكالمة حقيقية (٣ دقائق عيّنة، متحدثان). النتائج:

| المحرك | الزمن | النتيجة |
|---|---|---|
| `gemini` | **1012 ثانية** | فشل — 503 ازدحام ثم 404 تقاعد |
| `groq` | 3 ثوانٍ | نجح، ١٢٠ كلمة — **لكن الجودة ضعيفة** |

ثلاثة استنتاجات مؤكَّدة:

1. **زمن السقوط بين الموديلات كان قاتلًا** — ٥ محاولات × عدة موديلات = ١٧
   دقيقة قبل الفشل. أُصلح: `_post_json` صار يأخذ `max_retries` (يبقى ٥ لـ
   `ask.py` حيث لا بديل عن الطلب)، والسلسلة تمرّر ٢ فقط. الانتظار لكل موديل
   ٣٠٠ث ← ٢٠ث. يُضبط بـ `GEMINI_RETRIES`.
2. **خطأ التقاعد يسمّي البديل صراحةً** — نص الـ 404 يقول
   `use models/gemini-3.5-flash-lite`. صارت السلسلة تقرأ الاسم من الخطأ
   وتجرّبه فورًا، فتُصلح نفسها عند أي تقاعد لاحق بلا تحديث يدوي.
3. **ثقب هلوسة Whisper مؤكَّد بالدليل، لا نظريًا.** مخرَج Groq بدأ بـ
   «مرحباً. مرحباً. مرحباً. يا عبد البر. هل حياتك جميلة؟» — كلام لم يُقَل،
   وبالفصحى لا بالعامية. ثم استقام لاحقًا («عشان انا عندي جدو تحت»).
   هذا يؤكد بندي ٦ في القسم ٩، ويؤكد أن Whisper يُفصّح رغم الـ prompt.
   **`qwencleo` — وهو محور المشروع — لم يُختبر بعد.** اختباره هو الأولوية.
| `fetch_audio.py` | تنزيل من Drive/Dropbox/OneDrive | يُترجم، **لم يُختبر برابط حقيقي** |
| `summarize.py` | جدول ملخص Markdown لـ Actions | يُترجم |
| `.github/workflows/transcribe.yml` | تشغيل من أي متصفح | YAML صالح، **لم يُشغَّل على GitHub** |
| `.github/workflows/ask.yml` | سؤال الأرشيف من أي متصفح | YAML صالح، **لم يُشغَّل** |
| `README.md` | دليل المالك بالعربية | مكتوب |

المحركات المتاحة في `PROVIDERS`: `qwencleo`, `elevenlabs`, `gemini`, `groq`, `local`.

المخرجات لكل تسجيل في `journal/`: `.md` (للقراءة) و`.txt` (نص متصل)
و`.srt` (ترجمة متزامنة) و`.json` (وهو ما يقرأه `ask.py`).

---

## ٤. قيود تقنية مكتشفة — لا تكتشفها من جديد

1. **QwenCleo يحتاج GPU ليكون عمليًا.** runners GitHub Actions المجانية
   **بلا GPU**؛ عليها يكون أبطأ من الزمن الحقيقي فلا يصلح لتسجيل ساعة.
   البيوت الصحيحة له: **Colab (T4 مجاني)** أو **EC2 g4dn spot** (~سنت
   لكل ساعة صوت). أما `gemini`/`groq`/`elevenlabs` فلا تحتاج GPU وتعمل
   على Actions بلا مشكلة.
2. **رفع الملفات عبر متصفح GitHub محدود بـ ٢٥ ميجا.** لهذا وُجد
   `fetch_audio.py`: يرفع المالك التسجيل على Drive من زر المشاركة في
   الموبايل ويلصق الرابط. هذا هو المسار الموصى به للملفات الكبيرة.
3. **حدود الطبقات المجانية:** Groq ٢٥ ميجا للملف. Gemini 2.5 Flash ٢٥٠
   طلبًا يوميًا. ElevenLabs المجاني ~٣٠ دقيقة شهريًا (١٠٠٠٠ رصيد ÷ ٣٣٠
   للدقيقة).
4. **وظيفة Actions الواحدة:** ٦ ساعات كحد أقصى. الحساب المجاني ٢٠٠٠ دقيقة
   شهريًا للمستودعات الخاصة.
5. **ffmpeg** مثبت مسبقًا على runners أوبنتو. على ويندوز:
   `winget install --id Gyan.FFmpeg -e`. الدالة `find_binary()` تبحث في
   PATH ثم في مجلدات winget ثم في `FFMPEG_DIR`.

---

## ٥. اكتشافات خصوصية — مهمة، والمالك يعرفها

المحتوى قد يشمل تفاصيل نفسية أو جنسية شخصية. هذه أمور تحقّقنا منها:

1. **Gemini API الطبقة المجانية:** المحتوى قد يُقرأ بمراجعين بشريين
   ويُستخدم في تطوير منتجات Google. الطبقة المدفوعة لا. تكلفة تفريغ ساعة
   بـ 2.5 Flash ~١٠ سنتات.
2. **Bedrock:** لا يدرّب على بياناتك ولا يشاركها مع مزوّد الموديل. **لكن**
   وثائق AWS تنص أن Claude Fable 5/5.1 **تُحتفظ حركتها ٣٠ يومًا لكشف إساءة
   الاستخدام، والمُعلَّم منها يخضع لمراجعة بشرية محتملة**. النماذج الأقدم
   من Fable 5 بلا احتفاظ.
3. **محركات ASR لا ترفض محتوى ولا تحكم عليه** — Whisper وQwenCleo يكتبان ما
   يُقال. قلق الرفض ليس في طبقة التفريغ بل في طبقة التحليل فقط.
4. الحل الوحيد لـ«لا احتفاظ ولا مراجعة ولا رفض» هو موديل مفتوح الأوزان على
   سيرفر المالك. تقدير التكلفة: ٢–٦ دولار شهريًا على EC2 spot.

**المالك قرر تأجيل الخصوصية وتقديم الدقة حاليًا.** لا تعد فتح النقاش، لكن
لا تُزل هذه الملاحظات من `README.md`.

---

## ٦. المطلوب منك — مرتّبًا بالأولوية

### مهمة ١ — حسم «أي محرك أدق» على صوت المالك (الأولوية العليا)

هذا هو السؤال المركزي وهو **غير محسوم**. لا تحسمه من بنشمارك.

```bash
pip install -r requirements.txt
python compare.py <عيّنة من تسجيل المالك> \
  --minutes 3 --providers qwencleo,gemini,groq
```

- شغّله على GPU (Colab أو EC2) حتى يعمل `qwencleo`.
- أضف `elevenlabs` لو توفّر مفتاح — فهو المرجع الأوثق.
- **معيار القبول:** ملف `comparison.md` فيه جدول صفًّا بصف، وتوصية مكتوبة
  بمحرك واحد مع أمثلة نصية تبرّر الاختيار (أين حفظ العامية وأين فصّحها،
  وأين اخترع كلامًا).
- عيّن الفائز محركًا افتراضيًا في `transcribe.py` و`transcribe.yml`.

### مهمة ٢ — تشغيل حقيقي من طرف إلى طرف

`compare.py` و`ask.py` و`fetch_audio.py` والـ workflows **لم تُشغَّل قط**.

- شغّل workflow «تفريغ تسجيل» على تسجيل حقيقي عبر رابط Drive.
- تحقق أن `journal/` امتلأ بالملفات الأربعة، وأن `inbox/` نُظِّف.
- شغّل workflow «ناقش الأرشيف» بسؤال حقيقي.
- **معيار القبول:** تشغيلان ناجحان على GitHub، وتفريغ يقرأه المالك ويقول
  إنه يشبه كلامه.
- أصلح ما ينكسر. توقّع مشاكل في: تنزيل روابط Drive، وترميز UTF-8 في
  مخرجات Actions، وتحليل الطوابع الزمنية في `parse_timestamped()`.

### مهمة ٣ — بيت مجاني فيه GPU يعمل من أي مكان

الوصول من أي مكان + `qwencleo` + مجانًا = يحتاج GPU مجانيًا.

- ابنِ **نوتبوك Colab** يستنسخ المستودع، يثبّت التبعيات، ويعرض واجهة Gradio
  فيها سحب وإفلات، ويكتب المخرجات إلى Drive المالك.
- أو **HF Space**، مع ملاحظة أن ZeroGPU الموثوق يحتاج اشتراك Pro.
- **معيار القبول:** رابط واحد يفتحه المالك من موبايله، يرفع تسجيلًا، ويأخذ
  التفريغ — بلا تثبيت وبلا سطر أوامر.

### مهمة ٤ — تحسين الدقة بمرحلة تصحيح ثانية

نمط مثبت لتقليل الأخطاء على اللهجات: بعد التفريغ، أعطِ موديلًا قويًا
**الصوت + النص المسودة** واطلب تصحيح ما سُمع خطأً فقط، بلا إعادة صياغة
وبلا تفصيح.

- نفّذها كخيار `--refine` في `transcribe.py`.
- **معيار القبول:** قياس على نفس العيّنة يثبت تحسنًا فعليًا، أو تقرير يقول
  إنها لم تُحسّن ولماذا. لا تُدمج بلا قياس.

### مهمة ٥ — أتمتة الاستقبال (اختيارية، والمالك طلبها)

المالك يريد «يرمي التسجيل وينساه». الآن يجب أن يفتح Actions ويلصق رابطًا.

الحل المقترح والمفضّل: سكربت **Google Apps Script** صغير يراقب مجلدًا في
Drive، وأول ما يهبط فيه تسجيل يُشغّل الـ workflow عبر GitHub API.

> السبب في أن Apps Script يبعث رابطًا فقط ولا يفرّغ بنفسه: لا ffmpeg فيه،
> وحد الملف ٥٠ ميجا، وحد التنفيذ ٦ دقائق. أما Firebase Functions فيستطيع
> حمل ffmpeg لكنه يطلب خطة Blaze ببطاقة بنكية، والمالك يفضّل المجاني.

- **معيار القبول:** يرمي المالك ملفًا في مجلد Drive، ويجد التفريغ بلا أي
  خطوة يدوية.

---

## ٧. ما يحتاج قرار المالك — اسأله، لا تفترض

1. هل يقبل الدفع لـ ElevenLabs لو ثبت أنه الأدق على صوته بفارق واضح؟
2. هل عنده حساب AWS مهيأ (`aws configure` أو `aws sso login`)؟
3. مستودعه **خاص أم عام**؟ يجب أن يكون **خاصًا** — هذه تسجيلات حياته.
4. هل يريد أرشيف التفريغ في Git، أم في Drive، أم في S3؟

---

## ٨. كيف تتحقق من عملك

```bash
# ترجمة كل السكربتات
for f in transcribe.py ask.py compare.py fetch_audio.py summarize.py; do
  python -m py_compile "$f" || echo "FAIL $f"
done

# صحة الـ workflows
python -c "import yaml,glob;[yaml.safe_load(open(f,encoding='utf-8')) for f in glob.glob('.github/workflows/*.yml')]"

# المحركات مرئية
python -c "import transcribe;print(sorted(transcribe.PROVIDERS))"
```

---

## ٩. ثقوب معروفة لم تُعالَج — اعتبرها المهمة صفر

لم يُجرَ مسح منهجي لتقارير المشاكل التي وقع فيها ممارسون آخرون في نفس هذا
النوع من المشاريع. البحث الذي أُجري كان موجَّهًا (حدود المزودين، أسعارهم،
بنشمارك العربية، سياسات الخصوصية) وقد غيّر التصميم فعلًا مرتين. لكنه **ليس**
مسحًا لأدبيات الأخطاء العملية. هذه الثقوب التي أعرف أنها موجودة، فلا تكتشفها
بالطريقة المؤلمة:

1. **لا يوجد تطبيع للنص العربي إطلاقًا.** الألف بأشكالها (ا/أ/إ/آ)، والهمزة،
   والتاء المربوطة مقابل الهاء، والتشكيل، والتطويل — كلها تجعل نفس الكلمة
   عدة كلمات مختلفة. هذا يفسد البحث في الأرشيف، ويفسد أي حساب لـ WER، ويفسد
   إزالة التكرار. **نفّذ وحدة تطبيع عربي قبل أي قياس.**

2. **لم يُقس WER قط.** `compare.py` مقارنة بصرية فقط — لا نص مرجعي ولا مقياس
   رقمي. للحكم الجدّي: فرّغ ٥ دقائق يدويًا كنص مرجعي، ثم اقس WER/CER على
   نص مطبَّع.

3. **التقطيع بلا تداخل.** القطع يقع عند صمت (وهذا يخفّف كثيرًا) لكن الممارسة
   الشائعة تضيف تداخلًا ١–٣ ثوانٍ بين المقاطع مع إزالة التكرار عند الحدود،
   حمايةً من فقدان كلمة على الحد. غير منفَّذ.

4. **طوابع Gemini الزمنية غير موثوقة على الصوت الطويل** — انزياح تدريجي
   مشكلة مُبلَّغ عنها كثيرًا، و`parse_timestamped()` يعتمد عليها كليًا.
   تحقّق منها بمقارنتها بطوابع Groq/Whisper على نفس المقطع قبل الاعتماد عليها.

5. **لا يوجد diarization حقيقي.** تمييز المتحدثين في المكالمات يعتمد الآن على
   «أذن» الموديل عبر الـ prompt، وهذا ضعيف مع التداخل الكلامي. الأداة الجدّية
   هي `pyannote.audio`. أما ElevenLabs فعنده diarization أصلي مستخدَم بالفعل.

6. **هلوسة Whisper على الصمت والدخول في تكرار لانهائي** مشكلة معروفة.
   خُفِّفت بـ `condition_on_previous_text=False` و`vad_filter=True` — لكن من
   معرفة عامة، لا من تحقّق مقابل حالات مُبلَّغ عنها.

7. **رقم «توفير ٧٣٪» مضلِّل ويجب تصحيحه.** قِيس على ملف اختبار مُصنَّع
   (نغمة جيبية 128kbps ستيريو). نسبة التوفير تتبع بتريت المصدر: تسجيل موبايل
   64kbps أحادي سيوفّر أقل بكثير. **الرقم الموثوق هو المخرَج المطلق:
   ~١١–١٢ ميجا لكل ساعة صوت** — وهو ثابت لأنه يتبع إعداداتنا لا إعدادات
   المصدر. لم يُختبر ملف حقيقي ١٠٠ ميجا قط.

**المطلوب:** امسح تجارب الآخرين المنشورة (مشاكل تفريغ العربية الطويلة،
هلوسة Whisper، انزياح طوابع الـ LLM، تطبيع العربية، diarization للمكالمات)،
وأدخل ما يثبت نفعه، وصحّح هذا القسم بما وجدت.

---
## ١٠. قواعد سلوك

- **لا تُعِد بناء ما هو جاهز.** اقرأ الملف قبل أن تكتبه.
- **لا تدّعِ نجاحًا لم تُشغّله.** كل صف في جدول القسم ٣ يقول بصراحة ما
  اختُبر وما لم يُختبر — حافظ على هذه الأمانة وحدّثه.
- **لا تُدخل تبعيات ثقيلة بلا داعٍ.** المشروع يعتمد على `requests` فقط في
  مساره الأساسي، وهذا مقصود.
- **راسل المالك بالعربية.** هو يقرأ بها، والمشروع كله موثّق بها.
- **لا تضع أي مفتاح في الكود.** المفاتيح في `.env` محليًا، وفي
  GitHub Secrets على السحابة. `.env` مستثنى في `.gitignore` بالفعل.
